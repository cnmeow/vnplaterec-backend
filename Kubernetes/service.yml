apiVersion: v1
kind: Service
metadata:
  name: vnplaterec-backend
  labels:
    app: vnplaterec-backend
spec:
  type: NodePort
  ports:
  - port: 2106
    targetPort: 2106
    nodePort: 30007
  selector:
    app: vnplaterec-backend